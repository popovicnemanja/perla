import Contact from "@/components/Contact/Contact";

export const metadata = {
  title: "Perla | Kontakt",
};

const ContactPage = () => {
  const metadata = {
    title: "Perla | Kontakt",
  };

  return (
    <main>
      <Contact />
    </main>
  );
};

export default ContactPage;
