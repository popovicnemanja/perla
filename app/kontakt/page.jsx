import Contact from "@/components/Contact/Contact";

const ContactPage = () => {
  const metadata = {
    title: "Perla | Kontakt",
  };

  return (
    <main>
      <Contact />
    </main>
  );
};

export default ContactPage;
